# Write a PS script which writes out each second word from the lines of a given file!
# (string.split) (In UNIX you can use cut or sed or awk)Get-Content .\file.txt | ForEach-Object {$second =$_.Split()[1]Write-Host $second}